# Ocean-v1.3.3.2 生产音效

53 组差异化音效统一生成 OGG 与 MP3，共 106 个运行文件。浏览器读取 `audio-manifest.json` 后优先尝试 OGG，失败时改用 MP3，两者均失败才使用 Web Audio 合成回退。构建脚本校验 Linux 文件路径大小写；首次交互预载核心音效，进入对战预载战斗音效。隐藏武器仍不按命中或伤害改变声音，确保不泄露私人情报。背景音乐继续使用 `ocean-theme.mp3`。
